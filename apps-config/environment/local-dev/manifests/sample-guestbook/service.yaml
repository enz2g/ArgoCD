apiVersion: v1
kind: Service
metadata:
  name: guestbook-backend
  namespace: sample-guestbook
spec:
  selector:
    app: guestbook
  ports:
    - port: 32080        
      targetPort: 3000 
      protocol: TCP
  type: ClusterIP   