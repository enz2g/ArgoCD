apiVersion: v1
kind: Service
metadata:
  name: guestbook-backend
  namespace: sample-guestbook
  annotations:
spec:
  externalTrafficPolicy: Local
  selector:
    app: guestbook
  ports:
    - port: 80         
      targetPort: 3000 
      protocol: TCP
  type: LoadBalancer   